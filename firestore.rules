rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow public read on all collections, but restrict writes to authenticated users.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

  }
}
