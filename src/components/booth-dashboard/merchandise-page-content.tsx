'use client';

// This component is no longer used as its functionality has been merged
// into the POS page. It is kept here to avoid breaking imports but is empty.
export function MerchandisePageContent() {
  return null;
}
